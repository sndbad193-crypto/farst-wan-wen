## 2025-05-14 - Fix for Malformed HUD Attributes and Delete Confirmation
**Learning:** Correcting malformed HTML attributes where ID and Class are incorrectly combined (e.g., `id='hy-hud text-sm'`) is essential for maintaining functional DOM selection and accessibility standards in this project's legacy markup. Implementing confirmation dialogs for destructive actions like record deletion is a core project standard to prevent accidental data loss. Always be extremely precise with global string replacements (like `sed`) to avoid structural regressions in unrelated parts of the HTML.
**Action:** Always verify ID and class separation in legacy components and ensure all destructive UI actions are gated by a confirmation step. Perform line-specific or context-aware replacements instead of global ones to maintain HTML integrity.
