## 2024-05-22 - [Structural Integrity and Accessibility]
**Learning:** Legacy structural tag errors and malformed attributes (like using `</nav>` for scripts and combined `id` and `class` values) can disrupt functional DOM selection and accessibility tools.
**Action:** Standardize HTML structure and attribute separation to ensure consistent behavior across mirrored application files.
